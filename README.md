# Private family strategy

This repository publishes an encrypted static document for private family discussion.

Only ciphertext is stored in the HTML artifact. The decryption password is shared separately and is not stored in this repository.
